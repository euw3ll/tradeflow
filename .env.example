# ——— Telegram Bot ———
TELEGRAM_BOT_TOKEN=123456:ABCdefGHI_jklMNOpqrSTUvwxYZ
ADMIN_TELEGRAM_ID=123456789
# Opcional: canal/grupo para receber relatórios de erros (ID numérico, ex.: -1001234567890)
ERROR_CHANNEL_ID=

# ——— Criptografia (Fernet) ———
# Gere com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=YOUR_FERNET_KEY_BASE64

# ——— Telethon (leitura de sinais) ———
API_ID=0000000
API_HASH=aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa

# ——— Banco de Dados ———
# Em docker-compose, o serviço do bot usa POSTGRES_* para montar o DATABASE_URL
POSTGRES_USER=tradeflow
POSTGRES_PASSWORD=strong_password
POSTGRES_DB=tradeflow

# Se rodar o bot fora do docker-compose, defina diretamente (comente/descomente conforme uso)
# DATABASE_URL=postgresql://tradeflow:strong_password@localhost:5432/tradeflow

# ——— Logs e Startup ———
# Pasta onde serão criados logs/app.log e logs/errors.log
LOGS_DIR=logs
# Tentativas e intervalo (segundos) para alembic upgrade head no boot
MIGRATIONS_MAX_TRIES=20
MIGRATIONS_RETRY_SLEEP=3

# ——— Ajustes opcionais do motor ———
# Ticks de segurança ao ajustar SL na Bybit
TF_SAFETY_TICKS=2

